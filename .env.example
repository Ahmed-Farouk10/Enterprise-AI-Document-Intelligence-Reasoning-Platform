# --- Enterprise AI Platform Environment Variables (Template) ---
ENV=production
LOG_LEVEL=INFO
JSON_LOGS=true

# --- LLM Configuration (GEMINI - Enterprise Ready) ---
LLM_PROVIDER=gemini
LLM_MODEL=gemini/gemini-2.0-flash
# Get API Key from Google AI Studio: https://aistudio.google.com/
LLM_API_KEY=your_gemini_api_key_here
COGNEE_LLM_PROVIDER=gemini
COGNEE_LLM_MODEL=gemini/gemini-2.0-flash

# --- Embedding Configuration ---
EMBEDDING_PROVIDER=fastembed
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
COGNEE_EMBEDDING_PROVIDER=fastembed

# --- Storage Configuration (Persistent) ---
COGNEE_ROOT_DIR=/app/.cache/cognee_data
COGNEE_DATA_ROOT=/app/.cache/cognee_data

# --- HuggingFace Specific ---
# HF_TOKEN=hf_...  # Required for Inference API if not using Gemini

# --- Optional External Services ---
# NEO4J_URI=bolt://localhost:7687
# NEO4J_USER=neo4j
# NEO4J_PASSWORD=password
